#include<stdlib.h>
#include<conio.h>
#include<stdio.h>

 struct veiculos
 {
     char placa[10];
     char marca[20];
     char modelo[[15];
     int ano;
     float preço;

 } carro;

main()
{
 file *Automoveis;
 struct carro;

 if (fopen("c:\car.dad","r+b")) == null   {fopen("c:\car.da","w+b");}

 do
 {
     printf ("Informe a placa do veiculo");
     scanf ("%s",&carro.placa);
     printf ("Informe marca do  veiculo");
     scanf ("%s",&carro.marca);
     strupr(carro.marca);
     printf ("Informe o modelo do veiculo");
     sacanf
     printf ("Informe a ano");
     scanf ("%d",)
     printf ("Informe ")

     fwrite (&carro,siizeof(carro),1,veiculos);

     printf ("Deseja cadsatrar outro veiculo? ");
     scanf "%c",&opcao);
  }
 while



 fclose(atomovel)

 }
