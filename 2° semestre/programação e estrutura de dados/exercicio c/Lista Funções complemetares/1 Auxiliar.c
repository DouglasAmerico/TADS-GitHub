#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
#define n 10

main()
{
    printf ("Informe a frase\n");
    char frase[n],frase2[n];
    gets (frase);
    gets (frase2);
    if ((strlen(frase)) == (strlen(frase2)) {printf ("certo");}
    getch();
}
